# QueryAssist
QueryAssist helps with two SQL workflows:

- `POST /analyze-query` explains the impact of an existing SQL change statement.
- `POST /generate-query` accepts a plain-English requirement, inspects schema/lookups on its own, and returns one SQL statement.
